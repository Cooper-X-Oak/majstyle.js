# 📦 最终版本说明

## ✅ 唯一正确的版本

**文件位置**：`dist/雀魂金玉四麻风格分析助手-v2.0.0.user.js`

**构建时间**：2026-03-14 21:15

**文件大小**：30KB

**包含功能**：
- ✅ 原有的风格分析功能
- ✅ 数据探索模式（Ctrl+Alt+E）
- ✅ 所有 bug 修复

---

## ❌ 已删除的旧版本

**之前的位置**：根目录的 `雀魂金玉四麻风格分析助手-v2.0.0.user.js`

**构建时间**：2026-03-14 18:58（旧）

**状态**：已删除，避免混淆

---

## 📝 如何安装

### 方法 1：拖拽安装（最简单）

1. 找到文件：`dist/雀魂金玉四麻风格分析助手-v2.0.0.user.js`
2. 直接拖拽到浏览器窗口
3. Tampermonkey 会弹出安装页面
4. 点击"安装"按钮

### 方法 2：Tampermonkey 导入

1. 点击 Tampermonkey 图标
2. 点击"管理面板"
3. 点击"实用工具"标签
4. 在"从文件导入"处选择文件：`dist/雀魂金玉四麻风格分析助手-v2.0.0.user.js`
5. 点击"安装"

### 方法 3：复制粘贴

1. 用文本编辑器打开：`dist/雀魂金玉四麻风格分析助手-v2.0.0.user.js`
2. 全选复制（Ctrl+A, Ctrl+C）
3. 打开 Tampermonkey 管理面板
4. 点击"+"号创建新脚本
5. 粘贴内容（Ctrl+V）
6. 保存（Ctrl+S）

---

## 🔍 如何验证安装成功

### 检查版本

1. 打开 Tampermonkey 管理面板
2. 找到"雀魂金玉四麻风格分析助手"
3. 查看版本号：应该是 **v2.0.0**
4. 查看更新时间：应该是 **2026-03-14 21:15** 左右

### 测试功能

1. 进入雀魂对局
2. 按 `Ctrl+Alt+E`
3. 打开控制台（F12）
4. 应该看到"数据探索模式已触发"的输出

---

## 🛠️ 构建流程（开发者参考）

```bash
# 修改源代码后，运行构建命令
npm run build

# 构建结果会输出到 dist 目录
# 文件：dist/雀魂金玉四麻风格分析助手-v2.0.0.user.js
```

**重要**：
- 源代码在 `src/` 目录
- 构建后的文件在 `dist/` 目录
- **只使用 dist 目录的文件**
- 不要直接修改 dist 目录的文件

---

## 📂 项目结构

```
majstyle.js/
├── src/                    # 源代码（开发时修改这里）
│   ├── main.js            # 入口文件
│   ├── api/               # API 调用
│   ├── analysis/          # 分析逻辑
│   ├── game/              # 游戏交互
│   └── ui/                # UI 组件
├── dist/                   # 构建输出（使用这里的文件）
│   └── 雀魂金玉四麻风格分析助手-v2.0.0.user.js  ✅ 最终版本
├── docs/                   # 文档
├── tools/                  # 工具
├── rollup.config.js       # 构建配置
└── package.json           # 项目配置
```

---

## ❓ 常见问题

### Q: 我之前安装的是根目录的版本，需要重新安装吗？

A: 是的，建议重新安装 `dist/` 目录的版本。步骤：
1. 在 Tampermonkey 中删除旧版本
2. 安装 `dist/雀魂金玉四麻风格分析助手-v2.0.0.user.js`

### Q: 如何确认我安装的是最新版本？

A: 进入对局后按 `Ctrl+Alt+E`，如果控制台有输出，就是最新版本。

### Q: 为什么之前根目录有个版本？

A: 那是之前的构建输出位置，现在已经改为 `dist/` 目录，旧文件已删除。

### Q: 我修改了源代码，如何生成新的脚本？

A: 运行 `npm run build`，新的脚本会生成在 `dist/` 目录。

---

## 📌 总结

- ✅ **唯一正确的版本**：`dist/雀魂金玉四麻风格分析助手-v2.0.0.user.js`
- ❌ **已删除的旧版本**：根目录的同名文件
- 🎯 **安装方法**：拖拽到浏览器，或在 Tampermonkey 中导入
- 🔍 **验证方法**：进入对局按 `Ctrl+Alt+E` 测试

现在去安装 `dist/` 目录的版本吧！
